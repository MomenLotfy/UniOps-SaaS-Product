You are working inside an existing production system:
UniOps Control Tower — Internal Developer Platform (IDP)
⚠️ CRITICAL RULES:
DO NOT modify DevOps Center UI (Epic 6 is frozen)
DO NOT break existing APIs or auth system
DO NOT delete or refactor existing modules
ONLY ADD new backend platform engine capabilities
All changes must be additive and backward compatible
🎯 MISSION
Transform UniOps into a fully automated:
🚀 Internal Developer Platform (IDP) (ArgoCD + GitOps + Vercel-like deployment engine)
🧱 1. DATABASE LAYER (AUTO MIGRATION ON STARTUP)
Create new tables:
1. catalog_services
Fields:
id (UUID)
name
type (microservice/database/worker/queue)
stack (node/python/go/java)
status (creating/building/deploying/deployed/failed/synced)
repo_url
cluster_id
namespace
helm_path
argocd_app_name
created_at
updated_at
2. deployment_logs
Fields:
id
service_id
step_name
status (success/failure/running/skipped)
duration_ms
error_message
timestamp
⚙️ 2. NEW BACKEND MODULE STRUCTURE
Create:

backend/app/core/deployment_engine/
FILES:
1. generators.py
Responsible for generating:
Dockerfile
GitHub Actions CI / GitLab CI
Helm chart (full 5-file structure)
ArgoCD Application manifest
All outputs must be templated and deterministic.
2. git_provider.py
Implement:
GitHubProvider
GitLabProvider
Must support:
create_repository()
push_files()
commit_batch()
Use async httpx only.
If provider not configured → skip step gracefully and log it.
3. argocd.py
Implement ArgoCDClient:
create_application()
sync_application()
get_status()
Must poll sync/health status every 10 seconds.
4. service.py (CORE ENGINE)
Class: DeploymentEngine
MUST implement 10-stage pipeline:
PIPELINE FLOW:
Validate request
Persist service (status=creating)
Create Git repository
Push Dockerfile + README
Push CI/CD pipeline
Push Helm chart
Push ArgoCD manifest
Register ArgoCD application
Trigger ArgoCD sync
Start background tracking
REQUIRED BEHAVIOR:
At EACH step:
Write deployment_logs entry
Emit WebSocket event
Update service status in DB
📡 3. WEB SOCKET EVENTS (CRITICAL)
Emit via existing ws_manager:

service.created
service.repo_created
service.building
service.deploying
service.deployed
service.synced
service.failed
Each event must include:
service_id
step_name
timestamp
status
🔄 4. BACKGROUND WORKER
File:

worker.py
Responsibilities:
Poll ArgoCD every 10 seconds
Detect stuck deployments
Resume incomplete services on restart
Update DB status
Emit WebSocket updates
Must run with FastAPI startup event (no Celery, no Redis).
🌐 5. REST API (/api/v1/catalog)
Implement endpoints:
GET /services
pagination
filter by status/type
search by name
POST /services
triggers DeploymentEngine pipeline
returns HTTP 202 Accepted immediately
runs async in background
GET /services/{id}
returns full service details
PATCH /services/{id}/status
admin override only
DELETE /services/{id}
soft delete (stop deployment tracking)
GET /services/{id}/logs
returns deployment_logs ordered by step sequence
GET /stats
returns:
total services
running
failed
deployed
syncing
🧠 6. FAILURE HANDLING (VERY IMPORTANT)
If ANY external system fails:
GitHub/GitLab failure → mark step skipped
ArgoCD failure → retry 3 times
Kubernetes failure → mark failed
NEVER crash pipeline.
Always:
log error
emit service.failed event
continue safe state
🔐 7. SAFETY DESIGN RULE
If integrations are NOT configured:
skip step safely
write log: "integration not available"
continue pipeline normally
NO hard failures allowed.
📊 8. ARCHITECTURE REQUIREMENTS
System must be:
async (no blocking calls)
stateless engine
event-driven via WebSocket
DB is source of truth
GitOps is reconciliation layer
🚀 9. ACCEPTANCE CRITERIA
System is complete only if:
✔ Service created from API triggers full pipeline
✔ Git repo + CI + Helm are generated
✔ ArgoCD app is created
✔ Deployment is synced
✔ WebSocket shows live progress
✔ Logs are stored per step
✔ Failures do not crash system
✔ Restart resumes unfinished deployments
🧠 FINAL SYSTEM VISION
After Epic 7, UniOps becomes:
🚀 Fully automated Internal Developer Platform where developers deploy infrastructure without DevOps intervention