# Override for local dev — disables heavy optional services
# backend uses entrypoint.sh (db-wait + alembic + seed + uvicorn)
services:

  celery_worker:

  celery_beat:
    profiles: ["disabled"]

  flower:
    profiles: ["disabled"]

  prometheus:
    profiles: ["disabled"]

  grafana:
    profiles: ["disabled"]
